// Deployment Configuration// Copy this file to `config.js` and set your backend URL before deploying the

// To deploy:// static frontend (e.g., GitHub Pages). Do NOT commit `config.js` with your

// 1. Copy this file to config.js in the same directory// production URL if you prefer to set it in CI or via an automated deployment.

// 2. Update the API_URL below to point to your deployed backend

// 3. Make sure to include config.js in your HTML before app.js// Example:

// window.API_URL = 'https://your-backend-url.deta.dev';

window.API_URL = 'https://umkm-forecasting-app-production.up.railway.app';

// Replace the placeholder below with your deployed backend URL and copy this

// Configuration Options:// file to `config.js` next to `app.js`. Ensure `config.js` is included before

// - Production (Railway): 'https://umkm-forecasting-app-production.up.railway.app'// `app.js` in `index.html`.

// - Local Development: 'http://localhost:8000'window.API_URL = 'https://umkm-forecasting-app-production.up.railway.app';

// - Custom deployment: Replace with your backend URL